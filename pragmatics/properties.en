
An Entity E has:
    a "Type" (type(E)),
    a "Representation" (repr(E)),
    "Predicates" (vars(E)).

A Group G has:
    a "Maximum" (max(G)),
    a "Minimum" (min(G)),
    a "Sorted Order" (sorted(G)),
    a "Reversed Order" (reversed(G)).
    a "Size" (len(G)).